---
import { Image } from 'astro:assets';
import portrait from '../images/portrait.jpg';

interface Props {
	classList: string
}

const {classList} = Astro.props
---

<Image src={portrait} class:list={["h-56 w-48 rounded-full", classList]} alt="A portrait of me" />
